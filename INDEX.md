# Project knowledge map

This project-owned map routes questions to canonical sources. Keep navigation
pointer-only: no copied facts, decisions, history, or actor-local/private topic
rows (including `.agents/local/**`). Authorized portable external references
remain subject to `PROTOCOL_RULES.md §P6` and `§P7`.

Consult relevant rows on demand, then read the source. This complements
`PROJECT_RULES.md §J6`, which inventories protocol files. Missing/stale entries
are not proof of absence; use the independent discovery and search recipes in
`PROTOCOL_RULES.md §P-Access`, including older/current/archived evidence.

| Topic / question | Canonical file | Section or record locator |
|---|---|---|
| How should project knowledge be discovered? | `.agents/PROTOCOL_RULES.md` | `### Project knowledge discovery` |
| How are project rules organized? | `.agents/PROJECT_RULES.md` | `## §J1 — Project identity` |
| Where is the protocol file inventory? | `.agents/PROJECT_RULES.md` | `## §J6 — File reference map` |

Add project-specific question → file → section/anchor or JSONL record/query
locators as needed. Update affected pointers in the same session as relevant
file/folder create/remove/rename/move or section/anchor changes.

## Optional folder navigation

Register folder INDEX files or README navigation sections here only when they
exist, with their path and section locator. Substantive README content remains
valid; only its navigation portion is pointer-only. No folders are required.
